function multiplicacao(n1: number, n2: number): number {
    return n1 * n2
}

console.log(multiplicacao(2, 3))

function saudacao(nome: string): string {
    return `ola ${nome} Como voce esta?`
}

console.log(saudacao("lucas"))